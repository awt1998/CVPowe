# CVPower runs 100% in the browser.
# There are NO secrets, NO API keys, and NO backend environment variables.
# This file exists only to document that intentional absence.
#
# If you ever add build-time public config, prefix it with NEXT_PUBLIC_ and
# keep it non-sensitive. Never introduce a server secret — it would violate
# the project's privacy-first, no-backend architecture.
